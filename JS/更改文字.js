let str ='照片格式为JPG,最小尺寸为300*300像素,最大尺寸为5M';
// pList[i].innerHTML = pList[i].innerHTML.replace("JPG","JPG/PNG");
// console.log(str.replace("JPG","JPG/PNG"));

for(let i=0;i<3;i++){
    setTimeout(()=>{
        console.log(i);
    })
}